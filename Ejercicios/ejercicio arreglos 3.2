ejercicio arreglos 3.2

a)Almacenar 100 números.
b)Localizar el número de mayor valor y el de menor valor, informar sus valores y sus posiciones.
c)Contar y sumar todos los números pares.

Genere un único algoritmo que resuelva las 3 consignas del ejercicio anterior.

Accion 3.2 es

	Ambiente

	arr:arreglo(1..100) de enteros
	x:entero
	min:entero
	max:entero
	pos_max:entero
	pos_min:entero
	suma:entero
	cont_par:entero

Proceso

Para x:=1 a 100 hacer
	arr(x):=0
finpara

suma:=0
cont_par:=0

min:=arr(1)
max:=arr(1)
pos_max:=1
pos_min:=1


Para x:=1 a 100 hacer
	
	esc("ingrese un numero")
	leer(arr(x))

	Si arr(x) > max entonces
		max:=arr(x)
		pos_max:=x
	sino
		Si arr(x) < min entonces
			min:=arr(x)
			pos_min:=x
		finsi
	finsi

	Si (arr(x) mod 2 = 0) entonces
		cont_par:=cont_par+1
		suma:=suma+arr(x)
	finsi

finpara

esc("el valor maximo,"max,"esta en la posicion "pos_max)
esc("el valor minimo,"min,"esta en la posicion "pos_min)

esc("la cantidad de numeros pares, "cont_par)
esc("la suma de numeros pares, "suma)

finaccion

