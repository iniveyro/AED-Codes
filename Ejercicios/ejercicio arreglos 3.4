ejercicio arreglos 3.4

Considerando un arreglo de 50 números enteros, confeccione un algoritmo para resolver las siguientes consignas:

a)Modificar el arreglo dado, de modo que todos sus elementos sean múltiplos de 3.
b)Crear otro arreglo que contenga los números que no cumplieron la condición.
c)Informar cuántos números cumplieron la condición.

Accion 3.4 es
	
	Ambiente
	
	arr:arreglo(1..50) de enteros
	cont:entero
	arr_sal:arreglo(1..50) de enteros
	x:=entero

Proceso

Para x:=1 a 50 hacer
	Si arr(x) mod 3 = 0 entonces
		cont:=cont+1
	sino
		Si arr(x) mod 3 <> 0 entonces
			arr_sal(x):=arr(x)

			Mientras arr(x) mod 3 <> 0 hacer
				arr(x):=arr(x)+1
			finmientras
		finsi
	finsi
finpara

esc("la cantidad de numeros que son multiplos de 3 es de," cont)

finaccion

